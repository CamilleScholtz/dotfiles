#
# /etc/lilo.conf: lilo(8) configuration, see lilo.conf(5)
#

lba32
compact
install=text
disk=/dev/sdb inaccessible
boot=/dev/sda

image=/boot/vmlinuz
        label=CRUX
        read-only
        root=/dev/sda3

# End of file
