#
# /etc/lilo.conf: lilo(8) configuration, see lilo.conf(5)
#

lba32
install=text
disk=/dev/sdb inaccessible
boot=/dev/sda
image=/boot/vmlinuz
        label=CRUX
        read-only
        append="root=/dev/sda2"

# End of file